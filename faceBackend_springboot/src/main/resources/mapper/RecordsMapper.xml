<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjnu.facebackend.mapper.RecordsMapper">
    <select id="getRecordsPage" resultType="com.zjnu.facebackend.pojo.Records">
        SELECT id , authenticity , time , confidence , ip
            FROM records
    </select>

</mapper>
